import { ScrollToUpDirective } from './scroll-to-up.directive';

describe('ScrollToUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToUpDirective();
    expect(directive).toBeTruthy();
  });
});
